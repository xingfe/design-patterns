Decorator pattern allow to change behavior of the object 
without use inheritance. Decorator is de facto alternative
way to inheritance. 

Decorator is similar to adapter except
that adapter is use to change one interface to another, 
decorator switch behavior without changing interface. 
 

Use when:
In other words this pattern allows separate client code from 
particular implementation.
 
