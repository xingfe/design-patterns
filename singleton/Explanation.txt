Singleton pattern gives client access to only one instance of class
and provide global method to do that.

Use when:
Use this appropach when class is memory expensive or its creation and
inicjalization is time expensive. 
